apiVersion: 1

providers:
  - name: TaskOrchestrator
    folder: Task Orchestrator
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
