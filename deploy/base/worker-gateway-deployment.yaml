apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker-gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: worker-gateway
  template:
    metadata:
      labels:
        app.kubernetes.io/name: worker-gateway
        app.kubernetes.io/component: worker-gateway
    spec:
      serviceAccountName: task-orchestrator
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: worker-gateway
          image: ghcr.io/gnix0/task-orchestrator-worker-gateway:dev
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
          envFrom:
            - configMapRef:
                name: task-orchestrator-config
            - secretRef:
                name: task-orchestrator-secrets
          env:
            - name: GRPC_PORT
              value: "8080"
            - name: METRICS_PORT
              value: "9102"
            - name: INSTANCE_ID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
          ports:
            - name: grpc
              containerPort: 8080
            - name: metrics
              containerPort: 9102
          volumeMounts:
            - name: tls-materials
              mountPath: /var/run/task-orchestrator/tls
              readOnly: true
          readinessProbe:
            grpc:
              port: 8080
            initialDelaySeconds: 3
          livenessProbe:
            grpc:
              port: 8080
            initialDelaySeconds: 10
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 256Mi
      volumes:
        - name: tls-materials
          secret:
            secretName: task-orchestrator-tls
